#!/usr/bin/env bash
set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$repo_root"

if [[ ! -d node_modules || ! -d vendor/bundle ]]; then
	echo "Orb dependency snapshot is incomplete; rerun .agents/setup" >&2
	exit 1
fi

echo "Orb dependencies are ready"
