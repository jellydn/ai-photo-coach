## Codebase Patterns
- VisionCamera v5 uses Nitro modules architecture (react-native-nitro-modules)
- Camera component props changed: `outputs` array instead of `photo={true}`, no `testID` prop
- Jest tests require manual mocks for all native modules (vision-camera, permissions, safe-area-context)
- iOS permissions require NSCameraUsageDescription, NSPhotoLibraryUsageDescription, and NSMotionUsageDescription in Info.plist
- Android permissions need CAMERA, READ_EXTERNAL_STORAGE, WRITE_EXTERNAL_STORAGE, READ_MEDIA_IMAGES in AndroidManifest.xml
- Use @react-native-async-storage/async-storage for simple persistence (onboarding state, settings)
- Pattern: Create separate screen components for each onboarding step with shared navigation interface
- Pattern: Store onboarding completion state in AsyncStorage with key `@onboarding_complete`
- Pattern: Use View-based overlays with `pointerEvents="none"` for camera composition guides
- Pattern: Use percentage-based positioning (33.33% margins) for rule-of-thirds grid to scale across aspect ratios
- Pattern: React Native StyleSheet uses `absoluteFill` (not `absoluteFillObject`)
- Pattern: Create separate display metadata (UI strings/icons) from functional config (thresholds)
- Pattern: Sensor hooks should use RxJS Subscription type from 'rxjs', not from 'react-native-sensors'
- Pattern: react-native-sensors uses lowercase sensor types: `SensorTypes.accelerometer` (not `Accelerometer`)
- Pattern: Unsubscribe from sensors using `.unsubscribe()` method (RxJS), not `.remove()`
- Pattern: Face detection uses normalized coordinates (0-1) converted to percentages for display
- Pattern: Face framing guidance priority: face detection > distance > headroom position
- Pattern: z-index layering: camera (0) < composition overlay (10) < face overlay (12) < horizon (15) < prompt pill (25) < header UI (20)
- Pattern: Coaching prompt priority: stability > level > framing > lighting > composition (early return pattern)
- Pattern: Debounce strategy: immediate first prompt, immediate clear to null, 500ms rate-limit between changes
- Pattern: Use ReturnType<typeof setTimeout> instead of NodeJS.Timeout for cross-platform timeout types

# Ralph Progress Log
Started: Mon Apr 27 15:40:54 +08 2026
---

## Mon Apr 27 15:45:00 +08 2026 - US-001
- What was implemented:
  - Bootstrap React Native 0.85.2 TypeScript project
  - Installed react-native-vision-camera@^5 with peer dependencies (nitro-modules, nitro-image)
  - Installed react-native-worklets-core for frame processors
  - Installed react-native-permissions for permission handling
  - Created CameraScreen component with friendly permission denied-state UI
  - Configured iOS Info.plist with camera and photo library usage descriptions
  - Configured Android AndroidManifest.xml with camera and storage permissions
  - Set up Jest mocks for native modules
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - App.tsx - Updated to use CameraScreen
  - src/screens/CameraScreen.tsx - New camera screen with permission handling
  - ios/AIPhotoCoach/Info.plist - Added camera and photo library permissions
  - android/app/src/main/AndroidManifest.xml - Added camera and storage permissions
  - jest.config.js - Added moduleNameMapper for mocks
  - jest.setup.js - Test setup configuration
  - __mocks__/ - Created mocks for react-native-permissions, react-native-vision-camera, react-native-safe-area-context
  - package.json - Added typecheck script
- **Learnings for future iterations:**
  - VisionCamera v5 uses a new Nitro-based architecture - requires react-native-nitro-modules
  - Camera component API changed significantly from v4: no `photo` prop, uses `outputs` array instead
  - When testing components with native modules, create mock files in `__mocks__/` and add moduleNameMapper in jest.config.js
  - iOS permission prompts require usage description strings in Info.plist
  - Use `check` and `request` from react-native-permissions for cross-platform permission handling
  - Pattern: Use `SafeAreaView` from react-native-safe-area-context for proper insets handling
---

## Mon Apr 27 16:15:00 +08 2026 - US-002
- What was implemented:
  - Created 3-screen onboarding flow with Concept, Modes, and Permissions screens
  - ConceptScreen: App introduction with feature highlights and icon-based visual presentation
  - ModesScreen: Grid of 8 shooting modes (Portrait and Travel enabled, others show "Coming soon")
  - PermissionsScreen: Dynamic permission handling for Camera, Photo Library, and Motion (iOS only)
  - Added @react-native-async-storage/async-storage for persisting onboarding completion state
  - Added react-native-sensors for future motion sensor integration (US-007, US-008)
  - Updated App.tsx to conditionally show onboarding on first launch
  - Added NSMotionUsageDescription to iOS Info.plist for motion permission
  - Created comprehensive Jest mocks for new dependencies
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - App.tsx - Added onboarding state management and conditional rendering
  - src/screens/onboarding/ConceptScreen.tsx - New onboarding step 1
  - src/screens/onboarding/ModesScreen.tsx - New onboarding step 2
  - src/screens/onboarding/PermissionsScreen.tsx - New onboarding step 3
  - src/screens/onboarding/OnboardingNavigator.tsx - Navigation wrapper for onboarding flow
  - src/storage/onboarding.ts - AsyncStorage wrapper for onboarding state
  - ios/AIPhotoCoach/Info.plist - Added NSMotionUsageDescription
  - jest.config.js - Added mocks for async-storage and sensors
  - __mocks__/@react-native-async-storage/async-storage.js - New mock
  - __mocks__/react-native-sensors.js - New mock
  - package.json - Added async-storage and sensors dependencies
- **Learnings for future iterations:**
  - Use separate components for each onboarding screen - makes testing and maintenance easier
  - AsyncStorage is simpler than MMKV for basic persistence needs and has better React Native community support
  - For iOS motion permission, NSMotionUsageDescription is required in Info.plist
  - Android doesn't need explicit motion permission for sensors (handled differently than iOS)
  - Pattern: Check permissions when PermissionsScreen mounts using useEffect to show current state
  - TypeScript union types with `keyof` are useful for strongly typed permission handling
  - Platform.OS checks work well for iOS-only features like motion permission
---

## Mon Apr 27 16:50:00 +08 2026 - US-003
- What was implemented:
  - Created PhotoStorage interface with save(photo, metadata), list(), delete(id) methods
  - Created LocalPhotoStorage implementation using @react-native-camera-roll/camera-roll for camera roll access
  - Created metadata persistence using react-native-mmkv for photo metadata (mode, score, subscores, timestamp, photoId)
  - Added comprehensive unit tests covering save/list/delete paths
  - Created mocks for react-native-mmkv (createMMKV function) and @react-native-camera-roll/camera-roll
  - Exported PhotoData type for consumers
  - Created barrel export (index.ts) for the storage module
  - Updated jest.config.js and jest.setup.js for new mocks
  - Updated tsconfig.json to exclude __mocks__ from type checking (to avoid conflicts with real modules)
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - src/storage/PhotoStorage.ts - New interface and types
  - src/storage/LocalPhotoStorage.ts - New implementation
  - src/storage/index.ts - Barrel export
  - __mocks__/react-native-mmkv.ts - Mock for MMKV with createMMKV
  - __mocks__/@react-native-camera-roll/camera-roll.ts - Mock for CameraRoll
  - __tests__/LocalPhotoStorage.test.ts - Comprehensive unit tests
  - jest.config.js - Added moduleNameMapper for new mocks
  - jest.setup.js - Added beforeEach hook to reset CameraRoll mocks
  - tsconfig.json - Exclude __mocks__ from type checking
  - package.json - Added react-native-mmkv and @react-native-camera-roll/camera-roll dependencies
  - yarn.lock - Updated with new dependencies
- **Learnings for future iterations:**
  - react-native-mmkv v4+ uses `createMMKV()` factory function instead of `new MMKV()` constructor
  - MMKV methods are `remove()` (not `delete()`) and `getString()` (not `get()`)
  - When mocking native modules for Jest, need to update both jest.config.js moduleNameMapper AND tsconfig.json exclude patterns
  - TypeScript `export type` only exports types for type-checking, not for use as values
  - CameraRoll.saveAsset returns { node: { id: string } } structure
  - Always exclude __mocks__ from tsconfig.json to prevent TypeScript from picking up mock types instead of real module types
  - Use `Promise<void>` for setTimeout resolves to satisfy TypeScript strict mode
---

## Mon Apr 27 17:05:00 +08 2026 - US-004
- What was implemented:
  - Created src/config/modes.ts with Mode type union for all 8 shooting modes
  - Defined ModeConfig interface with all required thresholds: autoCaptureScore, faceMinAreaPct, faceMaxAreaPct, stabilityThreshold, horizonToleranceDeg
  - Created modeConfig record with per-mode configuration values for portrait, food, travel, group, product, document, pet_kids, and night modes
  - Implemented helper functions: getModeConfig(), isModeEnabled(), getEnabledModes(), getDisabledModes()
  - MVP modes (portrait, travel) set to enabled; others show as "Coming soon"
  - Added extra config properties (showOverlays, faceFraming) for future features
  - Created barrel export at src/config/index.ts
  - Comprehensive unit tests covering all modes, threshold validation, and helper functions
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - src/config/modes.ts - Mode configuration types, constants, and helper functions
  - src/config/index.ts - Barrel export for config module
  - __tests__/modes.test.ts - Comprehensive unit tests
- **Learnings for future iterations:**
  - Use TypeScript union types for strongly typed mode selection (e.g., `type Mode = 'portrait' | 'travel'...`)
  - Store per-mode configuration in a Record<Mode, ModeConfig> for type-safe access
  - Export both the type and its const array (e.g., `Mode` type and `MODES` const) for iteration and type checking
  - Include `enabled` flag in config for feature flagging MVP vs future modes
  - Add extra derived properties (showOverlays, faceFraming) to config to drive UI behavior
  - Pattern: Create helper functions that wrap direct record access for better abstraction and testability
  - When importing types in tests, use `type ModeConfig` syntax to indicate type-only import
---

## Mon Apr 27 18:20:00 +08 2026 - US-005
- What was implemented:
  - Created src/config/modeMetadata.ts with display metadata (icon, title, description) for all 8 modes
  - Created ModeSelectorScreen.tsx with 2-column grid layout showing all 8 modes
  - Enabled modes (Portrait, Travel) are tappable and navigate to CameraScreen
  - Disabled modes show "Coming soon" badge and are non-interactive
  - Updated App.tsx with screen state management (onboarding | modeSelector | camera)
  - Updated CameraScreen to accept mode prop and display mode badge with icon and title
  - Added back button in CameraScreen to return to mode selector
  - All modes have accessibility labels and hints for screen readers
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - src/config/modeMetadata.ts - New display metadata module
  - src/config/index.ts - Added modeMetadata export
  - src/screens/ModeSelectorScreen.tsx - New landing screen component
  - src/screens/CameraScreen.tsx - Added mode prop, mode badge, back button
  - App.tsx - Added screen navigation state management
- **Learnings for future iterations:**
  - Pattern: Separate display metadata (UI strings/icons) from functional config (thresholds)
  - Use simple state machine in App.tsx for navigation instead of full navigation library for MVP
  - `type AppScreen = "onboarding" | "modeSelector" | "camera"` pattern provides type-safe routing
  - For simple apps, prop drilling mode selection is cleaner than Context/Redux
  - Accessibility: Always provide accessibilityLabel and accessibilityHint for interactive elements
  - Accessibility: Use accessibilityState={{ disabled: !enabled }} for disabled buttons
  - Pattern: Keep mode metadata (icon, description) separate from mode config (thresholds, enabled status)
---

## Mon Apr 27 19:15:00 +08 2026 - US-006
- What was implemented:
  - Created CompositionOverlay component at src/components/CompositionOverlay.tsx
  - Rule-of-thirds grid: 2 horizontal + 2 vertical lines at 33.33% intervals
  - Center crosshair marker (small plus sign) at frame center
  - Overlay uses View-based approach with absoluteFill for positioning
  - Percentage-based margins (33.33%) ensure proper scaling across phone aspect ratios
  - Toggles visibility based on modeConfig.showOverlays (Portrait and Travel enabled by default)
  - pointerEvents="none" allows touches to pass through to camera preview
  - Customizable line color, line width, center marker color, and center marker size via props
  - Updated CameraScreen to import and render CompositionOverlay
  - Bottom hint text now shows whether composition guides are active for current mode
  - Created comprehensive unit tests for CompositionOverlay component
  - All quality checks passing (typecheck, lint, test)
- Files changed:
  - src/components/CompositionOverlay.tsx - New composition overlay component
  - src/screens/CameraScreen.tsx - Added CompositionOverlay integration with mode-based toggling
  - __tests__/CompositionOverlay.test.tsx - Unit tests for overlay component
- **Learnings for future iterations:**
  - View-based overlays perform better than Skia/SVG for simple composition guides
  - Use `pointerEvents="none"` on overlay containers to allow touch pass-through to camera
  - Percentage-based positioning (marginTop: "33.33%") scales automatically across aspect ratios
  - React Native StyleSheet uses `absoluteFill` (not `absoluteFillObject`) for full-screen absolute positioning
  - Inline style warnings are acceptable for percentage-based dynamic positioning that can't be precomputed
  - Overlay z-index management: overlay (10) < header UI (higher) to ensure interactive elements work
  - Pattern: Pass testID prop through components for reliable testing with react-test-renderer
---

## Mon Apr 27 21:20:00 +08 2026 - US-007
- What was implemented:
  - Created sensor math module (src/sensors/math.ts) with pure functions:
    - computeRollFromAccel(): Calculates roll angle from accelerometer data using atan2
    - isLevel(): Checks if roll is within tolerance of level
    - lowPassFilter(): Applies smoothing to eliminate jitter
    - Constants: DEFAULT_FILTER_ALPHA (0.2), MIN_UPDATE_INTERVAL_MS (33 for 30Hz)
  - Created useHorizonLevel hook (src/sensors/useHorizonLevel.ts):
    - Subscribes to accelerometer at >= 30Hz
    - Computes roll angle and applies low-pass filter
    - Returns roll, isLevel status, and rawRoll for debugging
    - Uses modeConfig.horizonToleranceDeg for per-mode tolerance
  - Created HorizonIndicator component (src/components/HorizonIndicator.tsx):
    - Rotating horizon line that follows device roll angle
    - Turns green when level, white when tilted
    - Shows center dot for pivot point visualization
    - Configurable via modeConfig.showHorizon
  - Updated modeConfig (src/config/modes.ts):
    - Added showHorizon boolean flag to ModeConfig interface
    - Enabled horizon indicator for all 8 modes (Portrait and Travel for MVP)
  - Updated CameraScreen (src/screens/CameraScreen.tsx):
    - Integrated useHorizonLevel hook with mode-specific tolerance
    - Added HorizonIndicator overlay
    - Updated bottom hint text to show horizon status
  - Added comprehensive unit tests:
    - sensors.test.ts: 29 tests covering math functions, edge cases, and constants
    - HorizonIndicator.test.tsx: 9 tests for component rendering and props
  - All quality checks passing (typecheck, lint, 73 tests)
- Files changed:
  - src/sensors/math.ts - New sensor math utilities
  - src/sensors/useHorizonLevel.ts - New sensor hook
  - src/sensors/index.ts - Barrel export
  - src/components/HorizonIndicator.tsx - New horizon indicator component
  - src/config/modes.ts - Added showHorizon flag to all modes
  - src/screens/CameraScreen.tsx - Integrated horizon indicator
  - __tests__/sensors.test.ts - Unit tests for sensor math
  - __tests__/HorizonIndicator.test.tsx - Unit tests for component
- **Learnings for future iterations:**
  - react-native-sensors uses RxJS Observables, not direct callbacks
  - SensorTypes uses lowercase keys: `SensorTypes.accelerometer` (not `Accelerometer`)
  - RxJS subscriptions use `.unsubscribe()`, not `.remove()`
  - Import Subscription type from 'rxjs', not from 'react-native-sensors'
  - atan2(y, z) correctly computes roll angle from accelerometer
  - Low-pass filter with alpha=0.2 provides good balance between responsiveness and smoothing
  - Percentage-based lineWidth (e.g., "60%") works well for cross-device compatibility
  - z-index layering: camera (0) < composition overlay (10) < horizon (15) < header UI (higher)
---

## Mon Apr 27 22:00:00 +08 2026 - US-008
- What was implemented:
  - Created stability detection module with pure functions in src/sensors/math.ts
  - computeAccelMagnitude() and computeGyroMagnitude() helper functions
  - computeStability() function using variance of sensor magnitudes over rolling 500ms window
  - Algorithm: weighted combination (accel 0.7 + gyro 0.3) compared against threshold
  - Created useStability() hook subscribing to both accelerometer and gyroscope at 30Hz
  - Integrated useStability into CameraScreen with mode-specific thresholds
  - Updated bottom hint text to show stability status ("Hold steady..." when unstable)
  - Priority: stability > level > composition in hint display order
  - Added 17 unit tests covering all threshold modes and edge cases
  - Updated react-native-sensors mock to use lowercase sensor types (pattern match)
- Files changed:
  - src/sensors/math.ts - Added GyroscopeData, SensorSample interfaces, computeStability(), computeAccelMagnitude(), computeGyroMagnitude(), STABILITY_WINDOW_MS, DEFAULT_STABILITY_THRESHOLD constants
  - src/sensors/useStability.ts - New hook for stability detection (accelerometer + gyroscope)
  - src/sensors/index.ts - Added export for useStability module
  - src/screens/CameraScreen.tsx - Integrated useStability hook, updated hint text logic
  - __mocks__/react-native-sensors.js - Fixed to use lowercase sensor types and unsubscribe() method
  - __tests__/sensors.test.ts - Added 17 tests for stability functions
  - __tests__/useStability.test.ts - New test file for stability module
  - scripts/ralph/prd.json - Updated US-008 to passes: true
- **Learnings for future iterations:**
  - Pattern: Use variance (average of squared differences from mean) to measure sensor stability
  - Pattern: Weighted combination of accelerometer (0.7) and gyroscope (0.3) for stability detection
  - Pattern: react-native-sensors SensorTypes uses lowercase keys: "accelerometer", "gyroscope"
  - Pattern: RxJS subscriptions use .unsubscribe(), not .remove()
  - Pattern: Stability threshold values: strict portrait = 0.02, loose travel = 0.05
  - Pattern: Rolling window approach - maintain array of samples, filter by timestamp on each update
  - Pattern: Priority order for prompts: stability > level > framing > lighting > composition
  - Gotcha: Variance calculation needs actual varying data in tests - identical samples have zero variance
  - Gotcha: Mock must match real library's API exactly (lowercase sensor types, unsubscribe method)
---

## Tue Apr 29 11:30:00 +08 2026 - US-009
- What was implemented:
  - Created face detection module (src/faceDetection/) with comprehensive types and utilities
  - Face detection types: DetectedFace, FaceBounds, FaceFramingGuidance, FaceDetectionResult
  - Utility functions: downscaleFrame(), calculateFaceAreaPercent(), selectPrimaryFace(), computeFaceFramingGuidance()
  - Created FaceOverlay component (src/faceDetection/FaceOverlay.tsx):
    - Displays face bounding box with color-coded border (green = good framing, yellow = needs adjustment)
    - Headroom guide line at upper third (33.33%) for composition guidance
    - Corner markers for visual appeal
    - Center marker at face center
    - Uses normalized coordinates (0-1) scaled to percentage positioning
  - Created useFaceDetection hook (src/faceDetection/useFaceDetection.ts):
    - Manages face detection state (faces, primaryFace, faceAreaPercent, framingGuidance)
    - Polling-based detection (stub for frame processor integration)
    - Per-mode threshold support via modeConfig
  - Integrated face detection into CameraScreen:
    - Shows FaceOverlay when modeConfig.faceFraming is enabled (Portrait mode)
    - Distance prompts displayed in bottom hint text (Step closer, Step back, Raise camera, Lower camera)
    - Priority order: stability > level > framing > lighting > composition
  - Added react-native-vision-camera-face-detector package for MLKit integration
  - Created mock for face detector plugin (__mocks__/react-native-vision-camera-face-detector.ts)
  - Comprehensive unit tests for types and utilities (22 tests in faceDetection.test.ts)
  - All quality checks passing (typecheck, lint, 129 tests)
- Files changed:
  - src/faceDetection/types.ts - Face detection types and utility functions
  - src/faceDetection/useFaceDetection.ts - Face detection hook
  - src/faceDetection/FaceOverlay.tsx - Face bounding box overlay component
  - src/faceDetection/index.ts - Barrel export for face detection module
  - src/screens/CameraScreen.tsx - Integrated face detection overlay and framing prompts
  - jest.config.js - Added moduleNameMapper for face detector mock
  - __mocks__/react-native-vision-camera-face-detector.ts - Mock for face detection plugin
  - __tests__/faceDetection.test.ts - Unit tests for face detection types and utilities
  - package.json - Added react-native-vision-camera-face-detector dependency
  - scripts/ralph/prd.json - Updated US-009 to passes: true
- **Learnings for future iterations:**
  - Pattern: Use normalized coordinates (0-1) for face bounds, then convert to percentages for display
  - Pattern: Face framing guidance priority: face detection > distance > headroom position
  - Pattern: downscaleFrame() maintains aspect ratio when scaling to max long edge (320px for ML)
  - Pattern: selectPrimaryFace() uses weighted scoring: 70% area + 30% centrality
  - Pattern: Headroom offset calculated as faceTop - upperThirdLine (0.333)
  - Pattern: FaceOverlay uses percentage-based positioning for cross-device compatibility
  - Pattern: Color-coded feedback: green = properly framed, yellow/orange = needs adjustment
  - Gotcha: React Native View style doesn't accept string percentages for left/top/width/height in TypeScript (use inline styles)
  - Gotcha: Frame processor integration with VisionCamera v5 requires native module setup and worklet context
  - Note: Face detection plugin (react-native-vision-camera-face-detector) installed but frame processor integration pending native build
---

## Tue Apr 29 12:00:00 +08 2026 - US-010
- What was implemented:
  - Created lighting analysis module (src/lighting/) with comprehensive types and utilities
  - Lighting types: LightingClass, LightingStats, LightingStatsWithRegions, LightingThresholds, HistogramStats
  - Pure functions for lighting analysis:
    - computeHistogramStats(): Calculates shadow/highlight percentages and clipping detection
    - calculateMeanLuminance(): Computes mean luminance from RGBA pixel data
    - extractLuminanceValues(): Extracts luminance values from pixel data
    - calculateRegionLuminance(): Calculates luminance for specific region (face area)
    - calculateBackgroundBrightness(): Calculates brightness excluding face region
    - classifyLighting(): Pure function classifying lighting as too_dark | too_bright | backlit | good
    - computeLightingStats(): Computes full lighting statistics from frame data
    - computeLightingStatsWithRegions(): Includes face region analysis for backlit detection
    - getLightingPrompt(): Returns user-friendly prompts (max 5 words)
  - Updated modeConfig (src/config/modes.ts):
    - Added lightingAnalysis boolean flag
    - Added lightingTooDarkThreshold (0-255) per mode
    - Added lightingTooBrightThreshold (0-255) per mode
    - Added lightingBacklitThreshold (ratio 0-1) per mode
    - Portrait: 40/220/0.6, Travel: 30/240/0.5 (more tolerant for outdoor)
  - Created useLighting hook (src/lighting/useLighting.ts):
    - Reactive lighting analysis with polling at target FPS (default 20)
    - Face bounds support for backlit detection
    - Per-mode threshold configuration
    - Returns lightingClass, stats, prompt, isGood, meanLuminance, isBacklit
  - Integrated lighting into CameraScreen:
    - Added useLighting hook with mode-specific thresholds
    - Passes face bounds for backlit detection in Portrait mode
    - Updated hint text priority: stability > level > framing > lighting > composition
    - Lighting prompts: "Too dark", "Too bright", "Face the light"
  - Constants: TARGET_LIGHTING_FPS = 20, MAX_LIGHTING_LONG_EDGE = 320
  - Comprehensive unit tests (36 tests in lighting.test.ts):
    - classifyLighting tests: too_dark, too_bright, backlit, good classifications
    - Edge case tests: boundary values, histogram clipping thresholds
    - Utility function tests: luminance calculation, region analysis
    - Constants verification
  - All quality checks passing (typecheck, lint, 158 tests)
- Files changed:
  - src/lighting/types.ts - Lighting analysis types and pure functions
  - src/lighting/useLighting.ts - Lighting analysis hook
  - src/lighting/index.ts - Barrel export for lighting module
  - src/config/modes.ts - Added lighting configuration (thresholds, flags)
  - src/screens/CameraScreen.tsx - Integrated useLighting hook
  - __tests__/lighting.test.ts - Comprehensive unit tests
  - scripts/ralph/prd.json - Updated US-010 to passes: true
- **Learnings for future iterations:**
  - Pattern: Use RGB to luminance conversion: Y = 0.299R + 0.587G + 0.114B (standard ITU-R BT.601)
  - Pattern: Shadow clipping threshold: >30% pixels in 0-20 range, Highlight: >25% in 235-255
  - Pattern: Backlit detection: faceBrightness/backgroundBrightness < 0.6 with >30 difference
  - Pattern: Frame processor stubs use polling at target FPS until real frame processor integration
  - Pattern: Lighting analysis priority in prompt chain: after framing, before composition
  - Pattern: Thresholds vary by mode (outdoor Travel more tolerant than indoor Portrait)
  - Pattern: Use Uint8Array for RGBA pixel data processing (efficient for frame analysis)
  - Pattern: Region-based analysis for backlit detection requires face bounds from face detection
  - Gotcha: Division by zero guard needed in brightness ratio calculation (backgroundBrightness || 1)
  - Gotcha: Histogram clipping thresholds use strict comparison (>), not (>=), so test data needs to exceed threshold
---

## Mon Apr 27 21:00:00 +08 2026 - US-011
 - What was implemented:
   - Created coaching module (src/coaching/) with pure functions for prompt selection
   - selectPrompt(): Pure function implementing priority order: stability > level > framing > lighting > composition
   - isReadyForCapture(): Pure function checking if all conditions satisfied for auto-capture
   - shouldUpdatePrompt(): Debouncing utility with 500ms default interval per spec
   - COACHING_PROMPTS constant object with all <= 5 word prompts per spec requirement
   - Created useCoaching() hook integrating all sensor/analysis hooks with debounced prompt output
   - Created PromptPill component with 200ms fade in/out animation at top-center position
   - Integrated coaching system into CameraScreen, replacing previous manual hint text logic
   - Updated CameraScreen to use isReady state for bottom hint text
   - Added comprehensive unit tests (36 tests) covering:
     - Priority ordering (stability > level > framing > lighting > composition)
     - Debouncing logic (immediate first prompt, 500ms interval, immediate clear)
     - Mode-specific context filtering (travel mode skips face framing)
     - isReadyForCapture() for all signal combinations
     - Prompt string requirements (all prompts <= 5 words)
   - All quality checks passing (typecheck, lint, 191 tests)
 - Files changed:
   - src/coaching/types.ts - Pure functions and types for prompt selection
   - src/coaching/useCoaching.ts - Coaching hook with debouncing
   - src/coaching/PromptPill.tsx - UI component with fade animation
   - src/coaching/index.ts - Barrel export
   - src/screens/CameraScreen.tsx - Integrated coaching system, added PromptPill
   - __tests__/coaching.test.ts - Comprehensive unit tests (36 tests)
   - scripts/ralph/prd.json - Updated US-011 to passes: true
 - **Learnings for future iterations:**
   - Pattern: Pure functions for prompt selection enable easy unit testing and deterministic behavior
   - Pattern: Debouncing logic: allow immediate first prompt, immediate clear to null, but rate-limit changes between prompts
   - Pattern: Priority-ordered prompt selection with early returns (guard clause pattern)
   - Pattern: CoachingContext enables mode-specific filtering (e.g., travel mode has no face framing)
   - Pattern: Use ReturnType<typeof setTimeout> instead of NodeJS.Timeout for cross-platform compatibility
   - Pattern: Fade animation with Animated API: useNativeDriver for smooth performance
   - Pattern: z-index layering: camera (0) < composition overlay (10) < horizon (15) < prompt pill (25) < header (20)
   - Pattern: Accessibility: use accessibilityRole="alert" and accessibilityLiveRegion="polite" for prompt announcements
   - Gotcha: When importing interfaces in barrel exports, ensure they are exported with 'export interface'
   - Gotcha: React Native Animated API requires useNativeDriver: true for opacity animations
   - Note: All 191 tests passing, including 36 new coaching-specific tests
 ---
