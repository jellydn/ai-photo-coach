---

## Fri May 01 17:00:00 +08 2026 - US-024
- What was implemented:
  - Enabled Pet/Kids mode in modeConfig (enabled: true, autoCaptureScore: 75, stabilityThreshold: 0.04, faceMinAreaPct: 12)
  - Added burstId field to PhotoMetadata interface for grouping burst shots
  - Extended autoCapture module with burst-mode support (UseAutoCaptureProps with burstMode, burstShotCount, burstIntervalMs)
  - Implemented 3-shot burst capture with 200ms intervals in useAutoCapture hook
  - Updated CameraScreen to handle burst capture flow with burst state management
  - Created burst carousel UI in PostCaptureScreen with dot indicators and swipe navigation
  - Added "Save All" / "Keep Best" / "Discard All" action buttons for burst mode
  - Added pet/kids specific coaching prompts: 'Get to their level', 'Wait for it…', 'Brace your phone'
  - Created PET_KIDS_MODE_WEIGHTS emphasizing framing (0.3) with reduced stability weight (0.15)
  - Updated App.tsx to handle burst photo data and pass to PostCaptureScreen
  - Updated telemetry to track burst capture events with isBurst and burstCount properties
  - Updated modes tests to reflect pet_kids enabled status
- Files changed:
  - src/config/modes.ts - Enabled pet_kids mode
  - src/storage/PhotoStorage.ts - Added burstId to PhotoMetadata
  - src/autoCapture/types.ts - Added burst mode props and constants (DEFAULT_BURST_SHOT_COUNT, DEFAULT_BURST_INTERVAL_MS)
  - src/autoCapture/useAutoCapture.ts - Implemented burst mode logic with burst state management
  - src/coaching/types.ts - Added pet/kids prompts and petKidsModeEnabled context
  - src/coaching/useCoaching.ts - Added petKidsModePrompt support
  - src/scoring/types.ts - Added PET_KIDS_MODE_WEIGHTS and petKidsModeEnabled to ScoreSignals
  - src/scoring/useScoring.ts - Added petKidsModeEnabled prop
  - src/scoring/index.ts - Exported PET_KIDS_MODE_WEIGHTS
  - src/screens/CameraScreen.tsx - Integrated burst mode, pet/kids prompts, and reordered hooks
  - src/screens/PostCaptureScreen.tsx - Added burst carousel UI and burst action buttons
  - App.tsx - Updated handlePhotoCaptured to support burst data and telemetry
  - src/telemetry/types.ts - Added burst tracking properties
  - __tests__/modes.test.ts - Updated to expect pet_kids enabled
- **Learnings for future iterations:**
  - Pattern: Burst capture requires careful hook ordering - capturePhoto callback defined before burst effect uses it
  - Pattern: Use module-level refs for burstId to persist across captures in same session
  - Pattern: Burst carousel: track burstPhotos array, current index, and keepAllBurst state
  - Pattern: PostCaptureScreen receives burstPhotos array and renders dot indicators + swipe navigation
  - Pattern: Burst mode actions: Save All (default), Keep Best (delete others), Discard All
  - Pattern: PhotoStorage already supports burstId through spread operator - no changes needed
  - Pattern: Auto-capture threshold for pet/kids (75) lower than other modes for aggressive capture
  - Pattern: PET_KIDS_MODE_WEIGHTS: framing 0.3, stability 0.15 (forgiving movement), level 0.15
  - Pattern: Coaching prompt priority for pet/kids: 'Brace your phone' (unstable), 'Wait for it…' (ready soon)
  - Pattern: Hook ordering matters: useScoring must be before petKidsModePrompt which uses score
  - Pattern: useCoaching must be after petKidsModePrompt which is passed as prop
  - Gotcha: React Hook ordering - signals must be declared before hooks that use them
  - Gotcha: burstId unused warning - prefix with underscore: _burstId
  - Gotcha: When updating mode enabled status, update all related tests (isModeEnabled, getEnabledModes, getDisabledModes)
  - Note: All 577 tests passing, typecheck and lint clean
---
