---

## Fri May 01 21:30:00 +08 2026 - US-025
- What was implemented:
  - Enabled Night Shot mode in modeConfig (enabled: true, autoCaptureScore: 70, stabilityWindowMs: 1500, countdownDuration: 5)
  - Added per-mode stabilityWindowMs and countdownDuration config fields to ModeConfig interface
  - Updated all 8 modes with stabilityWindowMs: 500 (default) and countdownDuration: 3 (default)
  - Night mode uses extended 1500ms stability window (vs 500ms default) for steadier shots
  - Night mode uses extended 5-second countdown (vs 3s default) for steadier hold
  - Added Night Shot mode coaching prompts: 'Find brighter spot', 'Hold very steady', 'Brace your phone'
  - Implemented lowLightStability subscore (0-100) that penalizes movement more in dark scenes (mean luminance < 60)
  - Created NIGHT_MODE_WEIGHTS with lowLightStability: 0.3 emphasis for night photography
  - Added LOW_LIGHT_LUMINANCE_THRESHOLD constant (60) for dark scene detection
  - Updated CameraScreen to pass stabilityWindowMs to useStability and countdownDuration to useAutoCapture
  - Updated CameraScreen to pass meanLuminance and nightModeEnabled to useScoring
  - Updated useLighting hook integration to extract meanLuminance for low-light scoring
  - Updated coaching system with nightModePrompt and nightModeEnabled context
  - Added unit tests for night mode in modes.test.ts and lowLightStability in scoring
- Files changed:
  - src/config/modes.ts - Added stabilityWindowMs and countdownDuration fields, enabled night mode
  - src/coaching/types.ts - Added night mode prompts (FIND_BRIGHTER_SPOT, HOLD_VERY_STEADY), nightModePrompt signals/context
  - src/coaching/useCoaching.ts - Added nightModePrompt prop and handling
  - src/scoring/types.ts - Added lowLightStability subscore, NIGHT_MODE_WEIGHTS, computeLowLightStabilityScore function
  - src/scoring/useScoring.ts - Added nightModeEnabled and meanLuminance props
  - src/scoring/index.ts - Exported NIGHT_MODE_WEIGHTS, computeLowLightStabilityScore, LOW_LIGHT_LUMINANCE_THRESHOLD
  - src/screens/CameraScreen.tsx - Integrated night mode prompts, extended stability window, countdown duration
  - __tests__/modes.test.ts - Updated to expect night mode enabled
  - __tests__/scoring.test.ts - Added lowLightStability to all SubScores test objects
  - __tests__/documentMode.test.ts - Added lowLightStability to SubScores test
  - __tests__/PostCaptureScreen.test.tsx - Added lowLightStability to defaultSubScores
- **Learnings for future iterations:**
  - Pattern: Per-mode config fields allow mode-specific behavior without hardcoded conditionals
  - Pattern: Adding new subscore requires updating: SubScores interface, ScoreWeights interface, all weight constants, computeScore function, computeWeightedScore, getSubscoreLabel, and test files
  - Pattern: Low-light stability scoring: detect dark scene via meanLuminance < threshold, apply 2x penalty vs regular stability
  - Pattern: Extended countdown (5s) gives users more time to steady their hold in challenging conditions
  - Pattern: Extended stability window (1500ms) ensures sustained stability before auto-capture in low-light
  - Pattern: Night mode weights emphasize lowLightStability (0.3) while reducing regular stability weight (0.15)
  - Pattern: Mode-specific prompts can be computed via useMemo with lightingClass, isStable, and score dependencies
  - Gotcha: When adding new subscore, update ALL test files with SubScores objects (scoring.test.ts, documentMode.test.ts, PostCaptureScreen.test.tsx)
  - Gotcha: Hook dependencies must include new props (nightModeEnabled, meanLuminance) in useEffect arrays
  - Note: All 578 tests passing, typecheck and lint clean (8 pre-existing warnings)
---

## Fri May 01 17:00:00 +08 2026 - US-024
- What was implemented:
  - Enabled Pet/Kids mode in modeConfig (enabled: true, autoCaptureScore: 75, stabilityThreshold: 0.04, faceMinAreaPct: 12)
  - Added burstId field to PhotoMetadata interface for grouping burst shots
  - Extended autoCapture module with burst-mode support (UseAutoCaptureProps with burstMode, burstShotCount, burstIntervalMs)
  - Implemented 3-shot burst capture with 200ms intervals in useAutoCapture hook
  - Updated CameraScreen to handle burst capture flow with burst state management
  - Created burst carousel UI in PostCaptureScreen with dot indicators and swipe navigation
  - Added "Save All" / "Keep Best" / "Discard All" action buttons for burst mode
  - Added pet/kids specific coaching prompts: 'Get to their level', 'Wait for it…', 'Brace your phone'
  - Created PET_KIDS_MODE_WEIGHTS emphasizing framing (0.3) with reduced stability weight (0.15)
  - Updated App.tsx to handle burst photo data and pass to PostCaptureScreen
  - Updated telemetry to track burst capture events with isBurst and burstCount properties
  - Updated modes tests to reflect pet_kids enabled status
- Files changed:
  - src/config/modes.ts - Enabled pet_kids mode
  - src/storage/PhotoStorage.ts - Added burstId to PhotoMetadata
  - src/autoCapture/types.ts - Added burst mode props and constants (DEFAULT_BURST_SHOT_COUNT, DEFAULT_BURST_INTERVAL_MS)
  - src/autoCapture/useAutoCapture.ts - Implemented burst mode logic with burst state management
  - src/coaching/types.ts - Added pet/kids prompts and petKidsModeEnabled context
  - src/coaching/useCoaching.ts - Added petKidsModePrompt support
  - src/scoring/types.ts - Added PET_KIDS_MODE_WEIGHTS and petKidsModeEnabled to ScoreSignals
  - src/scoring/useScoring.ts - Added petKidsModeEnabled prop
  - src/scoring/index.ts - Exported PET_KIDS_MODE_WEIGHTS
  - src/screens/CameraScreen.tsx - Integrated burst mode, pet/kids prompts, and reordered hooks
  - src/screens/PostCaptureScreen.tsx - Added burst carousel UI and burst action buttons
  - App.tsx - Updated handlePhotoCaptured to support burst data and telemetry
  - src/telemetry/types.ts - Added burst tracking properties
  - __tests__/modes.test.ts - Updated to expect pet_kids enabled
- **Learnings for future iterations:**
  - Pattern: Burst capture requires careful hook ordering - capturePhoto callback defined before burst effect uses it
  - Pattern: Use module-level refs for burstId to persist across captures in same session
  - Pattern: Burst carousel: track burstPhotos array, current index, and keepAllBurst state
  - Pattern: PostCaptureScreen receives burstPhotos array and renders dot indicators + swipe navigation
  - Pattern: Burst mode actions: Save All (default), Keep Best (delete others), Discard All
  - Pattern: PhotoStorage already supports burstId through spread operator - no changes needed
  - Pattern: Auto-capture threshold for pet/kids (75) lower than other modes for aggressive capture
  - Pattern: PET_KIDS_MODE_WEIGHTS: framing 0.3, stability 0.15 (forgiving movement), level 0.15
  - Pattern: Coaching prompt priority for pet/kids: 'Brace your phone' (unstable), 'Wait for it…' (ready soon)
  - Pattern: Hook ordering matters: useScoring must be before petKidsModePrompt which uses score
  - Pattern: useCoaching must be after petKidsModePrompt which is passed as prop
  - Gotcha: React Hook ordering - signals must be declared before hooks that use them
  - Gotcha: burstId unused warning - prefix with underscore: _burstId
  - Gotcha: When updating mode enabled status, update all related tests (isModeEnabled, getEnabledModes, getDisabledModes)
  - Note: All 577 tests passing, typecheck and lint clean
---
